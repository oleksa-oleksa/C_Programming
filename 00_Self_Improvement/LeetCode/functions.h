#ifndef FUNCTIONS_H
	#define FUNCTIONS_H FUNCTIONS_H

	// Deletes a rest of input after scanf
	void clearBuffer ();

	// Reads a text line
	void GetTextLine (char *);

	// User decides to quit 
	int askAgain();

	char* reverseString(char *);

	int* GetNumber (int *x);



	#endif